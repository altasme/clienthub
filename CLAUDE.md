# CLAUDE.md: Altaventures Client Hub

**Artifact type:** Build specification for Claude Code
**Functional source:** `CLAUDEclienthubcrm.md` (v2.1) is authoritative for scope, stage machine, security rules, and copy. This file documents the concrete implementation decisions made while building it — where they add detail, this file and the source spec should agree; if a future edit makes them diverge, treat the source spec as the intent and this file as the as-built record.
**Domain:** `account.altasme.com`
**Sibling app:** `clientkeeper` (`clientkeeper.altasme.com`) — the internal CRM. Separate repo, separate Cloudflare Pages deployment, **same D1 database**. See "Shared D1 database" below.
**Related:** the marketing site (`altasme/altaventureswebsite`) hosts `/foryourbusiness`, whose ₱299 ganap.net checkout is the entry trigger for this app (see "Where this sits in the ecosystem" below).

---

## 0. Where this sits in the ecosystem

- Separate application from the static marketing sites (`altasme.com`, `/foryourbusiness`, `/limitedoffer`). Those stay static and chat-first. This is a full app (D1, auth, server endpoints).
- Entry trigger: the ₱299 checkout on `/foryourbusiness` is paid via ganap.net. On confirmed payment, **this app's own webhook** (not the marketing site's) creates the client + project records and sends a payment-confirmation email (see §1 item 11 — it no longer issues a WorkOS invitation). This app owns everything from `payment_received` onward.
- **The marketing site's existing `/testpayment` + `orders`/`customers` D1 tables become legacy once this ships.** Today ganap.net's webhook for the `/foryourbusiness` project points at `https://altasme.com/testpayment` (in the marketing site repo). Once this app's own webhook handler (`functions/api/webhooks/ganap.ts`) is live and deployed, **repoint ganap.net's webhook URL** (same dashboard field already used twice for that project) to this app instead, so this app's `clients`/`projects`/`payments` tables become the one live source of truth. The marketing site's old tables and functions are deliberately left in place afterward (historical paid-order data shouldn't vanish) but stop being the active path — noted here so it isn't mistaken for an oversight later.
- Formalized flow: Payment (ganap.net) → account-creation email → account created → discovery call first → build → presentation → commercial unlock → ₱1,499 → Essential. Client stages are controlled in ClientKeeper.

---

## 1. Locked decisions (beyond the source spec)

1. **Two separate GitHub repos, two Cloudflare Pages projects, one shared D1 database.** Not a monorepo. A bug in this app's client-facing code has no code path into ClientKeeper's admin endpoints — the isolation is structural (separate deployments, separate Functions), not just a convention.
2. **This app owns every public-facing endpoint.** The ganap.net payment webhook, the WorkOS invitation-acceptance callback, and all client-scoped read/write endpoints live here. **ClientKeeper has zero public endpoints** — everything there requires a staff WorkOS session. This is the strongest version of the source spec's isolation goal (§1.1).
3. **Staff roles live in a D1 `users` table, not WorkOS Organizations/roles.** Researched WorkOS's real invitation/roles API against `workos/workos-node`'s source on GitHub (`workos.com` is blocked by this sandbox's proxy, same restriction hit building the marketing site's WorkOS integration): WorkOS *can* attach a `role_slug` to a user, but only via an Organization Membership — there is no role concept for a user outside an Organization. Standing up a WorkOS Organization purely to hang four role slugs (Owner/Developer/Admin/Sales) off it is unnecessary complexity for V1, which has no self-service "invite a staff member with a role" UI anyway. Roles are assigned by a one-time manual D1 insert per hire. This is exactly the fallback the source spec itself allows (§10: "if roles are not fully managed in WorkOS"). WorkOS is used purely to authenticate *who* is making a request; authorization (*what they can do*) is a local D1 lookup.
4. **All invitations (staff and client) are plain `POST /user_management/invitations` with just `email`** (no `organization_id`, no `role_slug`) — confirmed via the same SDK-source research: WorkOS sends the invitation email itself and also returns `accept_invitation_url` if a different delivery path is ever wanted. `expires_in_days` is available if a shorter/longer expiry than WorkOS's default is wanted later.
5. **Account-creation bridge uses two redundant signals**, mirroring the ganap.net webhook-vs-browser-redirect pattern already proven on the marketing site: the primary path is the OAuth `code` exchange at `/api/auth-callback` when the browser completes AuthKit signup and redirects back; the `invitation.accepted` webhook (confirmed as a real WorkOS event via the same SDK research) is a secondary, idempotent backstop that catches acceptance even if the browser redirect never lands (closed tab, network drop, etc.). Both signals match the WorkOS user to its `clients` row by email **case-insensitively** (`LOWER(email) = LOWER(?)`) — caught live in testing: WorkOS normalizes emails to lowercase on signup, while the email stored from ganap.net's payload is only trimmed, not lowercased (whatever capitalization the customer typed at checkout), so an exact-match comparison intermittently failed for the very same address.
6. **Booking system: RESOLVED [2026-09-07], real engine built — see §10.** This originally shipped as interim behavior (client submits preferred times, staff calls manually) per the source spec's own default (§11 open item #2), pending a real spec. That spec arrived; §10 documents the real booking engine that replaced it. Kept here for history: the interim behavior no longer exists in the codebase.
7. **Resource/ebook library deferred to V1.1**, per the source spec's own flag that it's the most deferrable V1 item — confirmed with the user. No `resources`/`resource_access` tables in the V1 schema; add them when this is scheduled.
8. **Chat handoff reuses the marketing site's pattern, not its code** (separate repo — no cross-repo import). `src/lib/contact.ts` here has its own `wa.me`/`viber://`/`m.me` URL builders, seeded from the same numbers as `altaventureswebsite`'s `CONTACT` constant. Keep both in sync by hand if the numbers ever change.
9. **One WorkOS login, one business — confirmed with the user.** A `clients` row is 1:1 with a `workos_user_id` (enforced by a `UNIQUE` constraint), and that's a deliberate choice, not just a schema default: if the same person pays for a second, separately-emailed order, that becomes a fully separate account, not a second business folded into their existing one. This surfaced as a real bug during testing: a client already logged into WorkOS from an earlier order clicked "Create Your Account" for a *second*, differently-emailed paid order, and WorkOS silently reused the existing browser session instead of prompting a real login — the person was invisibly logged into their *first* account, with the second paid order left sitting unlinked with no indication anything was wrong. Fixed in `functions/api/auth-start.ts` by adding `prompt=login` to the authorize URL, which forces WorkOS to always show a real login/signup screen rather than silently reusing an active session (confirmed as a genuine passthrough param via `workos-node`'s own source/tests, `workos.com` being proxy-blocked here as usual). Every account-creation attempt is now a conscious choice of which email to sign in with.
10. **`screen_hint` fix [2026-09-06]: a first-time client was landing on the sign-in screen instead of sign-up.** `/api/auth-start` never set WorkOS's `screen_hint` param, and per `workos-node`'s own SDK (its `authkit-nextjs` package's `getScreenHint()` explicitly falls back to `'sign-in'` unless told `'sign-up'`), that means AuthKit's hosted UI defaulted to sign-in for every caller — including a client's very first visit right after paying, via the marketing site's "Create Your Account" link, who has no account yet. Fixed by reading an optional `?intent=signup` query param on `/api/auth-start` and only then adding `screen_hint=sign-up` to the authorize URL; the marketing site's `ForYourBusinessThankYouPage.tsx` now links to `/api/auth-start?intent=signup`. This file's other two callers — `src/lib/MeContext.tsx`'s two "unauthenticated, bounce to login" redirects, for a session-expired *returning* client — deliberately omit `?intent`, so they keep getting the (correct, default) sign-in screen. `screen_hint` is a genuine passthrough param on the `authorize` endpoint, confirmed against `workos-node`'s own interface types and test suite (`workos.com` itself proxy-blocked here, as usual).
11. **"One way in" fix [2026-09-07]: the ganap webhook no longer sends a WorkOS invitation at all.** Item 4 above (`POST /user_management/invitations`) meant WorkOS emailed its own `accept_invitation_url` automatically the instant a payment cleared — at the same moment the marketing site's thank-you page showed its own "Create Your Account" button (item 10's `?intent=signup` link). A real client hit both at once and got confused about which was the actual way to sign up, since either one worked and completed the exact same account. Per the operator's explicit instruction, there must be exactly one entry point: the thank-you page's button. `functions/api/webhooks/ganap.ts` no longer calls WorkOS's invitations API in any form — `functions/_lib/workos.ts`'s `sendInvitation()`/`WorkosInvitation` were deleted outright as a result (zero remaining callers in this repo). In its place, the webhook sends a plain **payment-confirmation email** (`functions/_lib/email.ts`, via Resend, best-effort) whose only account-creation mention is a small footer notice linking to the same `?intent=signup` URL — never the headline of the email, and omitted entirely for a returning client who already has a linked account (they get a dashboard link instead). This required no change to the actual linking logic (`functions/_lib/accountBridge.ts`): it already matched a WorkOS user to a `clients` row purely by email against an unlinked, paid row, with no dependency on an invitation token ever having existed — so self-serve signup via `/api/auth-start?intent=signup` links correctly with no invitation in the picture at all. The `invitation.accepted` webhook (`functions/api/webhooks/workos.ts`) is left in place as a harmless, now-dormant backstop — it only ever fires for a WorkOS invitation, and none get created through this app's own flow anymore, but it costs nothing to keep for the rare case of a staff member manually inviting someone from the WorkOS dashboard directly. `clientkeeper`'s "Resend Account Invitation" action (`functions/api/app/clients/[id]/resend-invite.ts`) was changed the same way — it now re-sends the same reminder email instead of a second WorkOS invitation; see that repo's CLAUDE.md.

---

## 2. Tech stack

Matches the marketing site's proven stack, so patterns and conventions transfer directly:

- **Frontend:** React 18 + Vite + TypeScript + Tailwind CSS.
- **Server:** Cloudflare Pages Functions (`functions/`) — the only layer bound to D1.
- **Database:** Cloudflare D1 (SQLite), shared with `clientkeeper`.
- **File storage:** Cloudflare R2 — deferred until the resource library (V1.1).
- **Auth:** WorkOS AuthKit, called via plain `fetch()` against the REST API (no Node SDK — Cloudflare Workers isn't a Node runtime), same approach as the marketing site's `functions/api/auth-callback.ts`.
- **Email:** WorkOS sends whatever auth emails its own hosted signup/login flow triggers (magic auth, password reset) — it no longer sends a client-facing invitation email at all (see §1 item 11). Resend sends every app/funnel email (payment confirmed, discovery reminder, etc.), including the one account-creation nudge that exists.
- **Hosting:** Cloudflare Pages, custom domain `account.altasme.com`.

No `pdf-lib`/`qrcode` here — those are marketing-site-specific (WSA-free PDF generation, `/foryourbusiness` QR payment rendering) and don't apply to this app.

---

## 3. Shared D1 database

`d1/schema.sql` in **this repo** is the canonical schema — `clientkeeper` reads/writes the same physical database, it does not get its own copy of this file. Apply it once:

```
npx wrangler d1 execute <database-name> --remote --file=./d1/schema.sql
```

(or paste into the D1 Console tab in the Cloudflare dashboard — if it rejects a multi-statement paste, split into smaller chunks, the same issue hit with the marketing site's simpler `orders`/`customers` schema).

**Both** Cloudflare Pages projects (`clienthub` and `clientkeeper`) must bind the *same* database id under variable name `DB` (Settings → Functions → D1 database bindings in each project). Every Function reads it as `env.DB`.

Tables (see `d1/schema.sql` for full definitions and comments): `users` (staff, role lives here), `leads`, `clients`, `businesses`, `project_stages` (static reference data, seeded in the schema file itself), `projects`, `stage_history`, `discovery_sessions`, `presentations`, `offers`, `offer_events`, `payments`, `subscriptions`, `client_activity`, `notifications`, `audit_log`.

No RLS — enforcement is entirely in the Functions layer of each app, per the source spec §6.

---

## 4. Stage machine

States: `payment_received, account_created, discovery, building, ready_for_presentation, presentation, post_presentation, offer_unlocked, conversion, essential_upsell`, plus `on_hold, cancelled, completed` reachable from any active state.

Allowed forward transitions: strictly the sequence above, one step at a time. The only way to break sequence is an admin override (a ClientKeeper-only action) that requires a reason and writes both `audit_log` and `stage_history`. Every transition — forward or override — writes `stage_history` with `actor_id` and (for overrides) `reason`.

The transition map is validated in application code (a shared TypeScript module, not recomputed from `project_stages` — that table is reference/display data only). Both this app and `clientkeeper` need the same map; until there's a real reason to share code across the two repos (there isn't yet — the two apps' server layers are deliberately independent per §1.2 above), keep the map duplicated in each repo's own stage-machine module rather than reaching for a shared package. Revisit only if the two copies actually drift.

---

## 5. Security (client-facing endpoints)

Every endpoint here must, per the source spec §6:

1. **Authenticate** the WorkOS AuthKit session.
2. **Authorize ownership** — the requested resource belongs to *this* authenticated client. Never trust an id from the request without checking it against the session.
3. **Stage-gate** — return commercial/offer data only when the client's stage has reached it. A locked offer returns nothing, not a hidden-in-the-UI value.

No endpoint here can change `stage`, `current_offer`, `current_plan`, or any other commercial column — those mutations exist only in `clientkeeper`. Profile edits (name, mobile, facebook, current_website) go through one dedicated endpoint that touches only those fields.

### 5.1 Session handling — a home-rolled cookie, deliberately not WorkOS's native session

Researched WorkOS's real session model against `workos-node`'s actual source (`workos.com` is proxy-blocked here): `access_token` is a short-lived JWT verified via `https://api.workos.com/sso/jwks/<client_id>`, refreshed via `POST /user_management/authenticate` with `grant_type=refresh_token`. WorkOS's own SDK wraps this in a "sealed session" cookie helper (`CookieSession`) built on `jose` + `iron-webcrypto` — both genuinely Web-Crypto-native, so technically usable in a Worker.

**Deliberately not used.** Pulling in `workos-node` for its session primitive risks dragging in its full HTTP client (used nowhere else in this app — everything else is plain `fetch()`, see `functions/_lib/workos.ts`) and Node-specific bundling issues beyond just that one piece. For a V1 client portal (not a banking app), `functions/_lib/session.ts` implements a simple HMAC-signed cookie instead, reusing the same `hmacSha256Hex`/`timingSafeEqual` primitives already proven for ganap.net/WorkOS webhook verification (`functions/_lib/crypto.ts`). Payload is minimal: `clientId`, `workosUserId`, `issuedAt` — a 30-day max age, `HttpOnly; Secure; SameSite=Lax`. **The real tradeoff, stated plainly:** if a client's WorkOS account is later suspended/revoked, this session keeps working until it naturally expires, rather than being invalidated immediately the way a WorkOS-native session would be. Acceptable for V1; revisit if that gap ever actually matters.

`/api/auth-callback` issues the cookie on successful account link (or re-link on a returning login). `/api/auth-logout` clears it. `functions/api/client/_middleware.ts` validates it on every route under `/api/client/*` (Cloudflare Pages Functions scopes a `_middleware.ts` to its own directory tree — `/api/auth-*` and `/api/webhooks/*` are untouched by it) and attaches `clientId`/`workosUserId` onto the request `data` object; every downstream client endpoint scopes its queries to that `clientId`, never to an id taken from the request itself.

**Failed-login state is surfaced, not silently retried.** `auth-callback.ts` redirects to `/?error=no_account` (no `clients` row matches the signed-in email — see guardrail below) or `/?error=auth_failed` (the code exchange or account link itself failed) rather than just bouncing back to the dashboard. `src/lib/MeContext.tsx` checks for `?error=` before doing anything else and, if present, renders a dedicated screen (headline + explanation + Try Again + Message Us) instead of its normal "unauthenticated → redirect to `/api/auth-start`" behavior. This was a real bug caught live: without this check, an error redirect immediately triggered another unauthenticated `/api/client/me` call, which redirected straight back into `/api/auth-start` — a silent, rapid infinite loop with no explanation ever shown to the customer.

**`no_account`'s "Try Again" sends the client back to sign-up, not sign-in [2026-09-07 fix].** Both error screens shared one `handleRetry` that always redirected to bare `/api/auth-start` (sign-in). For `no_account` specifically — "We couldn't find a matching order," meaning they completed a real WorkOS sign-in/signup but the email didn't match any paid `clients` row — sending them to a sign-in retry assumes they already have the right account when the actual problem is almost always that they entered the wrong email during account creation. `handleRetry` now branches on `code`: `no_account` → `/api/auth-start?intent=signup` (same signup entry point as everything else in the "one way in" fix above), `auth_failed` → unchanged plain `/api/auth-start`, since that's a generic exchange/session failure unrelated to email matching. Verified via Playwright against the built `dist/` (`vite preview`): clicking "Try Again" on `/?error=no_account` navigates to `/api/auth-start?intent=signup`; on `/?error=auth_failed` it navigates to plain `/api/auth-start`.

---

## 6. V1 scope — implemented

Per the source spec §39/§40, one primary action per stage. All of the below is built and locally verified (see §7's testing note):

- **`GET /api/client/me`**: the one read endpoint the dashboard needs — profile, most recent project's stage, and whatever discovery/presentation/offer data is relevant. Internal fields (`discovery_sessions.internal_notes`, a locked offer's `content`) are never selected in the query at all, not filtered out after the fact.
- **`POST /api/client/schedule`**: "Schedule a Call" / "Schedule Presentation" — the interim booking behavior (§1.6), upserting the one `discovery_sessions`/`presentations` row per project with the client's preferred times.
- **`POST /api/client/profile`**: the one profile-edit endpoint per §5 above. Email is deliberately not editable in V1 — it's the field the account bridge matches on, and there's no re-verification flow yet to safely let a client change it themselves.
- **`src/pages/DashboardPage.tsx`**: renders one primary action per stage exactly per the list below. `src/components/ProgressRail.tsx` shows Payment/Account/Discovery/Build/Presentation done-or-active, and collapses every commercial stage (`post_presentation`, `offer_unlocked`, `conversion`, `essential_upsell`) into a single generic "Next Steps" pill — verified by screenshot that no commercial stage name ever renders there.
  - **Discovery (default post-account):** "Talk to Your Developer" — Schedule a Call + Chat with Your Developer (`src/components/ChatModal.tsx`, opens Messenger/Viber/WhatsApp via `src/lib/contact.ts`).
  - **Building:** status only, no action.
  - **Ready for Presentation:** Schedule Presentation.
  - **Presentation:** status only + chat fallback.
  - **Post-presentation onward:** renders the client's unlocked `offers` row generically (`headline`/`price`/`body` from its JSON `content`) if one exists, a generic "we'll be in touch" message if not (no offer exists to unlock yet until `clientkeeper`'s unlock action is built).
  - **on_hold / completed:** simple status states.
- **`src/pages/WebsitePage.tsx`**: adapts by stage (Discovery / In Development / Ready / Live with a visit link using `project.website_url`), exists from day 1 per the spec.
- **`src/pages/AccountPage.tsx`**: name, business, email (read-only), mobile, Facebook, current website, created date. No internal CRM fields.
- **Resources:** deferred to V1.1 (see §1.7 above) — not built.

---

## 7. Definition of done (V1)

- [x] ganap.net webhook creates client + project + payment and sends a payment-confirmation email with an account-signup link (idempotent on `external_reference`, signature-verified). It no longer issues a WorkOS invitation — see §1 item 11.
- [x] Client rows exist at payment; the WorkOS user links on self-serve signup via `/api/auth-callback` (or, for the rare manually-invited case, the `invitation.accepted` webhook backstop). "Paid, no account" list + re-issue action live in `clientkeeper` (`resend-invite.ts`, now email-based per §1 item 11); the underlying data (`clients.invitation_status`) drives both.
- [x] A WorkOS user is only ever linked to a client with a matching paid order — never provision access from a WorkOS sign-in with no paid order behind it (verified: an `invitation.accepted` event, or a plain self-serve signup, for an email with no paid order creates no client link — `linkClientAccount` matches by email against an unlinked `clients` row regardless of how the WorkOS user got there).
- [x] Verified locally (see below) for this app's own endpoints: locked offer returns nothing; no client endpoint can mutate stage/offer/plan (structurally true — no such endpoint exists); D1 unreachable except through Functions; unauthenticated requests rejected (401 on every `/api/client/*` route with no/invalid session cookie). Cross-client id guessing is moot by construction — no client endpoint accepts a client/project id from the request at all, every query is scoped to the session's own `clientId`. The remaining acceptance tests (ClientKeeper rejecting unauthenticated/wrong-role requests) belong to that repo.
- [x] Client Hub shows exactly one "what do I do now" action per stage; no future commercial offers before unlock (verified: a `locked` offer row returns `null` from `/api/client/me`, confirmed by directly flipping a seeded offer's status in a local D1 and re-querying).
- [x] ₱1,499 offer content is DB-configurable (the schema and read path support it — `offers.content` is free-form JSON). `clientkeeper`'s unlock action now exists (built after this was originally written) and ships with clearly-marked placeholder ₱1,499/Essential copy until real content is supplied — see that repo's CLAUDE.md §3/open item #5. This app's dashboard rendering was verified against a placeholder offer row inserted directly into local D1.
- [x] Booking uses the real engine (`POST /api/client/schedule`, `GET /api/client/availability` — §10), verified end to end against a local D1; chat opens Messenger/Viber/WhatsApp. Resend now sends the payment-confirmation email (§1 item 11); the rest of the app-email set (discovery reminder, build started, website ready, etc.) is still not wired up — a later phase (§12).

**How this was tested:** entirely against a local `wrangler pages dev` + local D1 instance (gitignored `.dev.vars`/`wrangler.toml`, deleted after every session, never committed), since this sandbox cannot reach `api.workos.com` (same restriction hit throughout this project). A client was seeded end to end through the real webhook handlers (signed ganap.net payload → signed `invitation.accepted` payload), then a session cookie was hand-constructed using the same HMAC algorithm as `functions/_lib/session.ts` (the real cookie can only come from a live WorkOS OAuth exchange, which this sandbox can't perform) to drive Playwright screenshots of the dashboard across the discovery and post-presentation-with-offer states, the schedule modal, the website page (both "in progress" and "live" variants), and the account page. Test the real WorkOS OAuth handshake and cookie issuance end to end once deployed.

---

## 7.1 Required Cloudflare Pages environment variables

Set on the `clienthub` Cloudflare Pages project (never committed): `GANAP_SECRET` (the same signing secret already used by the marketing site's `/foryourbusiness` checkout project — this app's webhook is meant to replace that project's webhook target, not add a second secret), `WORKOS_API_KEY`, `WORKOS_CLIENT_ID`, `WORKOS_WEBHOOK_SECRET` (from registering `https://account.altasme.com/api/webhooks/workos` in the WorkOS dashboard, subscribed to at least `invitation.accepted` — kept only as the now-dormant backstop per §1 item 11), `SESSION_SECRET` (a new, randomly-generated secret for this app's own session cookie signing — see §5.1; not shared with any other app or secret), `RESEND_API_KEY`/`RESEND_FROM_EMAIL` (§1 item 11's payment-confirmation email; best-effort — a missing pair just skips the email, logged, since the client/project/payment rows already committed are what actually matters). Optional: `DB` (the shared D1 binding — see §3; required in practice, since every endpoint here depends on it, but Cloudflare only calls it "optional" in the sense that a missing binding degrades gracefully rather than crashing the build). Note `WORKOS_API_KEY` is no longer read by the ganap webhook itself (only by `/api/auth-callback`'s code exchange) — it stays required at the app level, just not for that one function.

**Added [2026-09-07], the Pricing page (§12):** `GANAP_INTERNAL_UPSELL_SECRET`, `GANAP_INTERNAL_UPSELL_PROJECT_UUID` — a completely separate ganap.net project (`alta_internal_upsell`) from the `/foryourbusiness` one above, its own signing secret, never reused.

---

## 8. Guardrails

1. No client endpoint can change stage, offer, plan, or any commercial column, ever.
2. Locked offers are not returned by any client endpoint — not merely hidden in the UI.
3. ~~The full pricing table (beyond the currently-unlocked offer) is never exposed to a client endpoint.~~ **AMENDED [2026-09-07]** — this predates the Pricing page (§12), which is a deliberate, separate catalog the operator asked to be self-serve browsable/purchasable from post_presentation onward; it is not the same thing as an `offers` row. The rule that still stands, unchanged: a *locked* `offers` row's content is never returned before it's unlocked (guardrail #2 above), and no client endpoint can ever write a stage/offer/plan value it wasn't itself explicitly authorized to write (guardrail #1) — `/api/client/checkout-upsell` only ever creates a *new* `subscriptions` row via the webhook, it never lets a client mutate an existing one directly.
4. Account creation only ever links a WorkOS user to a `clients` row with a matching paid order behind it — a self-serve signup (or, rarely, an accepted invitation) for an email with no such row creates no link (§1 item 11).
5. D1 is reachable only through this app's own Functions — no public DB endpoint, no client-side credentials.
6. ~~Do not build real booking-system scaffolding before its spec is supplied~~ **RESOLVED [2026-09-07]** — the real spec arrived and the engine is built; see §10.
7. Do not build the resource/ebook library before it's scheduled for V1.1.

---

## 9. First-login welcome modal [2026-09-07]

A one-time welcome message (operator-supplied copy: brand intro, what the Hub is for, an honest "still in early development" heads-up) shown to a client the first time they land in the Hub, dismissible permanently.

- **Persisted server-side, not in `localStorage`.** A client may open the Hub from more than one device/browser, and the requirement is "seen this once, ever" — a per-browser flag would just make it reappear on a new device. `clients.welcome_dismissed_at` (nullable `TEXT`, `d1/schema.sql`) is `NULL` until dismissed, non-`NULL` forever after. `GET /api/client/me` exposes it to the frontend as a plain boolean, `client.hasSeenWelcome` — the raw timestamp isn't useful there.
  - **This is a genuine schema migration, not just an edit to `d1/schema.sql`.** That file's `CREATE TABLE IF NOT EXISTS` is a no-op against the already-created live `clients` table, so the new column needs a one-time manual migration against the real database: `ALTER TABLE clients ADD COLUMN welcome_dismissed_at TEXT;` — run it via the D1 Console (or `wrangler d1 execute <database-name> --remote --command "..."`) before deploying this change. Confirmed via `wrangler d1 execute ... --local` end to end that a fresh `CREATE TABLE IF NOT EXISTS` picks the column up correctly for a brand-new database; only an *existing* database needs the manual `ALTER TABLE`.
- **`POST /api/client/welcome-dismiss`** (`functions/api/client/welcome-dismiss.ts`) stamps `welcome_dismissed_at = now()` for the session's own `clientId` — same ownership pattern as every other `/api/client/*` endpoint (never a client-supplied id), idempotent (safe to call more than once).
- **`WelcomeModal.tsx`** is mounted once, in `Layout.tsx` (the shared shell every route renders inside), gated on `!me.client.hasSeenWelcome`. Copy lives in `content/site.ts`'s `WELCOME_MODAL` constant, matching this app's existing content-as-data convention. Dismissing it — either the `×` button, the primary "Got it, thanks!" button, `Escape`, or a backdrop click, all routed through one `handleDismissWelcome` — does two things: an optimistic local `locallyDismissed` state hides it instantly (no flash while the network call is in flight), and the real `POST /api/client/welcome-dismiss` call + `refresh()` (from `useMe()`) makes the dismissal durable. A failed dismiss call is best-effort — logged, not surfaced to the client — since the only consequence is the modal reappearing next login, which is a trivial retry, not a broken state.
- **How this was tested:** live, end to end, against a local `wrangler pages dev` + local D1 instance (gitignored `wrangler.toml`/`.dev.vars`, deleted after the test session, never committed) — a test `clients` row was seeded directly with `welcome_dismissed_at` left `NULL`, a session cookie hand-constructed with the same HMAC algorithm as `functions/_lib/session.ts` (matching the pattern already established in §7's own "How this was tested"), then driven through Playwright: confirmed the modal renders on first load, clicking "Got it, thanks!" hides it and the D1 row's `welcome_dismissed_at` is stamped with a real timestamp (queried directly afterward), and a full page reload afterward does *not* re-show it.

---

## 10. Real booking engine [2026-09-07]

Replaces the interim "submit preferred times, staff calls you" behavior (§1 item 6, §8 guardrail 6 — both now resolved) with a real calendar: the client picks an exact discovery-call slot themselves against staff-defined availability; presentation scheduling stays entirely staff-driven, per the operator's explicit split, but shares the same conflict-checking grid so the two can never double-book.

**Fixed business rules** (confirmed with the operator, not configurable per-request): every session is **45 minutes** with a mandatory **15-minute buffer** after it, so the effective grid cadence is **60 minutes**. All times are **Asia/Manila (UTC+8, fixed — no DST)**. A client can book a discovery call **exactly once at a time** — rebooking always replaces the existing booking (upsert), never creates a second row — and must pick a **future** slot to reschedule into. Staff set a meeting link that only the client can see (never writable by the client themselves). Presentation date/time is **staff-set only**, never client-bookable.

**Availability model: a recurring weekly template, not individual dates** (confirmed with the operator over a manual date-by-date alternative) — `availability_rules` (new table, `d1/schema.sql`): `day_of_week` (0=Sunday..6=Saturday) + `start_time`/`end_time` ("HH:MM", Manila-local). A day can have more than one rule (e.g. a lunch-break split). No one-off exception/holiday-closure mechanism exists yet — deliberately out of scope for this pass; if the operator wants specific dates closed later, add a small `availability_exceptions` table rather than forcing it through the weekly template (which would affect every week, not just one).

**The math lives in `functions/_lib/scheduling.ts`** (duplicated into `clientkeeper` per this project's "reuse the pattern, not the code" convention — clientkeeper's copy is smaller, since it only needs the conflict rule, not the full slot-grid generator):
- `generateCandidateSlots(rules, now)`: every bookable slot start (UTC ms) within `BOOKING_HORIZON_DAYS` (21) that clears `MIN_LEAD_MINUTES` (120 — a client can't book a call starting in the next two hours). Manila-local calendar math is done with plain UTC-ms arithmetic (`+8h`/`-8h`), not an `Intl`/timezone-data lookup — a fixed, DST-free offset needs none of that, and it keeps the function pure/synchronous.
- **Conflict rule, derived once and reused everywhere**: two sessions conflict iff their start times are **less than 60 minutes apart, in either direction** (`slotsConflict`). This single symmetric check is mathematically equivalent to "no session may start inside another's 45-minute occupied window AND at least a 15-minute gap is kept on both sides" — treating every session as reserving a full 60-minute block starting at its own start time makes the two-sided gap requirement fall out of one comparison rather than needing separate before/after interval-overlap logic. Verified directly (`npx tsx`, no D1 needed): exact-same-time conflicts, 59 min apart conflicts, 60 min apart does not, 61 min apart does not — all four match the intended rule exactly.
- This conflict rule is what lets clientkeeper's presentation scheduling (staff typing an arbitrary time, not picking from a generated grid) share the same "no double-booking" guarantee as the client's own grid-constrained booking, with no separate interval-overlap logic needed for the free-form case.

**`GET /api/client/availability`** (`functions/api/client/availability.ts`) returns open discovery-call slots: `generateCandidateSlots()` against the live `availability_rules`, minus every `scheduled`, non-null `scheduled_at` across **both** `discovery_sessions` and `presentations` — deliberately **not** scoped to the caller's own `client_id`, since this is one shared, business-wide calendar (one team doing every discovery call and presentation), not a per-client resource. Only ever exposes slot start times, never who holds a taken slot.

**`POST /api/client/schedule`** (rewritten, same route as the old interim endpoint) is the one write path for a discovery booking — body `{ startTime }`. Upserts the caller's project's `discovery_sessions` row (create on first booking, update in place on reschedule — there is only ever one row, which is exactly the "book once, reschedule replaces it" rule) after **re-validating server-side against the live rules and current bookings**, never trusting a client-supplied time just because it once appeared in an availability response — time has passed and another client may have taken it. A last-moment conflict (`409`, excluding the caller's own existing session so rescheduling to a nearby time isn't blocked by itself) is a real, deliberately accepted small race window: two clients could in principle hit this within the same instant, but for a low-volume small-business booking system this is an acceptable tradeoff over distributed locking. Verified live against a local D1: booking, rescheduling (confirmed only one row ever exists via a direct query), rejecting a non-grid-aligned time (`400`), and rejecting a slot another client already holds (`409`, and confirmed that slot then disappears from `/api/client/availability`).

**`GET /api/client/me`** now also returns `discovery.meetingLink` / `presentation.meetingLink` (both nullable, staff-set only) alongside the existing `scheduledAt`/`externalStatus` fields.

**`src/components/BookingModal.tsx`** replaces the deleted `ScheduleModal.tsx` (which handled both discovery and presentation via a `type` prop and a plain preferred-times textarea) — discovery-only now, since presentation never gets a client modal at all. Fetches `/api/client/availability`, groups slots by Manila-local calendar day (`Intl.DateTimeFormat` with `timeZone: "Asia/Manila"` — safe here since this is browser-side *display* formatting only, not the authoritative slot math, which stays server-side in Workers-compatible plain arithmetic), and re-fetches the list on a `409` so a slot someone else just took doesn't sit there looking pickable. `DashboardPage.tsx`'s discovery card shows "Schedule a Call" before a booking exists and "Reschedule Call" after, plus the scheduled date/time and a "Join the call" link once `meetingLink` is set; the presentation cards (`ready_for_presentation`/`presentation` stages) lost their old client-facing scheduling button entirely and just display whatever staff set, once set.

**ClientKeeper side** (staff): `GET/POST /api/app/availability` + `DELETE /api/app/availability/:id` (`functions/api/app/availability/`) is plain CRUD on the weekly rules, no role restriction (matches this codebase's existing precedent — no endpoint anywhere restricts by specific staff role beyond "any authenticated staff"). A new **Availability** card on `SettingsPage.tsx` lists rules grouped by day with a small add-rule form (`<input type="time">`, which already returns "HH:MM" in the exact format the backend expects). `discovery/[id].ts` and `presentations/[id].ts`'s `PATCH` both gained a `meetingLink` field and now re-validate a staff-supplied `scheduledAt` against the same conflict rule (`409` on collision) — staff can override a discovery session's time directly, and it's just as protected against double-booking as the client's own booking flow. **`POST /api/app/presentations`** (new — this table previously had no creation endpoint at all, since only the now-removed client-side interim flow ever created one) creates the first presentation for a project: `{ projectId, scheduledAt, meetingLink? }`, conflict-checked against the shared grid, rejected `400` if the project already has one (update the existing row instead). `ClientDetailPage.tsx`'s Presentation card now shows a `SchedulePresentationForm` (plain `datetime-local` + meeting-link input) when no presentation exists yet, or a `RescheduleField` + `MeetingLinkField` alongside the existing status/notes editor once one does.

**How this was tested (ClientKeeper side):** live, end to end, against its own local `wrangler pages dev` + local D1 instance seeded from the same `d1/schema.sql` (gitignored `wrangler.toml`/`.dev.vars`, deleted after the session, never committed) — added an availability rule via the real endpoint and confirmed it round-trips through `GET`; rejected an invalid rule (`end_time` before `start_time`); created a presentation at a time that conflicted with an existing discovery session (`409`, correctly rejected) and at a clear time (`200`, correctly accepted); attempted a second presentation for the same project (`400`, correctly rejected — the "update instead" guardrail); confirmed `GET /api/app/clients/:id` returns both tables' `meeting_link`. Playwright screenshots confirmed `ClientDetailPage.tsx` renders the scheduled times, pre-filled meeting-link field, and reschedule input correctly, and that `SettingsPage.tsx`'s Availability card lists/adds/removes rules correctly.

**Known open items (not blocking, do not build speculatively):**
- No one-off availability exceptions (holidays, a single day off) — see the availability-model note above.
- `BOOKING_HORIZON_DAYS` (21) and `MIN_LEAD_MINUTES` (120) are reasonable operator-facing defaults, not confirmed numbers from the operator specifically — adjust in `functions/_lib/scheduling.ts` (both copies) if the real preference differs.
- Staff's `datetime-local` inputs (`RescheduleField`, `SchedulePresentationForm`) are interpreted in the *browser's own* local timezone, not forced to Asia/Manila — fine as long as staff are actually in the Philippines when entering a time, but not timezone-safe if that ever changes. Revisit with a proper Manila-locked time picker if staff ever operate from elsewhere.

## 11. Dashboard copy + stage visuals [2026-09-07]

Three small, purely presentational changes to `src/pages/DashboardPage.tsx`, all requested together as part of a larger "Client Hub-ClientKeeper improvements" batch (the other two items — a real purchasable pricing page and Account-page plan/renewal details — are separate, larger pieces of work tracked separately, not yet built).

- **Building stage** now shows a small looping "in progress" visual (`BuildingAnimation`, a browser-window-shaped frame with three `animate-bounce` dots — built entirely from Tailwind's existing bounce keyframe, no new dependency, no real screenshot since there's nothing real to show at this stage) alongside updated copy: "We are building your website. Average build time is 4–7 days. Once it's built, we'll present it to you."
- **Presentation stage** copy changed to "Your website is ready!" plus a line naming the scheduled date ("We've scheduled the presentation date on {day, date, time}") when `presentation.scheduledAt` is set. The meeting link is deliberately **withheld until the calendar day of the presentation itself** (`isPresentationToday()`, comparing Asia/Manila date-keys via `Intl.DateTimeFormat("en-CA", { timeZone: "Asia/Manila", ... })` on both the presentation's `scheduledAt` and `now` — a date-key comparison, not a 24-hour-window one, so "today" always means the presentation's own local day) — before that day, the client instead sees "We'll send you the meeting link via chat, or you'll see it here on the day of the presentation." Below that, `BlurredPreview` renders a heavily-blurred, purely-decorative CSS mockup (abstract bars/blocks, not a real screenshot standing in as one — this project's no-fabricated-screenshot guardrail is about real content faked as genuine, not an honestly-labeled teaser) captioned "Sneak peek — we don't want to spoil the surprise!", framed as the reason the real preview isn't shown yet.
- **Post-presentation stage's no-offer fallback** headline changed from "What's next?" to "Your website is now live!" — only the branch where `me.offer` is falsy; the branch that renders real DB-backed offer content (`me.offer.content.headline` etc.) is unchanged.

**How this was tested:** live, end to end, against a local `wrangler pages dev` + local D1 instance seeded from `d1/schema.sql` (gitignored `wrangler.toml`/`.dev.vars`, deleted after the session, never committed) driven via Playwright (`playwright-core` against the sandbox's preinstalled Chromium). Confirmed via screenshot: the building stage's copy and animation render correctly; the presentation stage with a **future** `scheduled_at` correctly hides the meeting link and shows the fallback copy + `BlurredPreview`; updating the same presentation's `scheduled_at` to today's Manila date and reloading correctly **reveals** the "Join the call →" link (`isPresentationToday()` verified true on the day, false before it); advancing the project's stage to `post_presentation` with no `offers` row present correctly renders "Your website is now live!".

---

## 12. Digital Growth Plans (Pricing page) [2026-09-07]

**⚠️ REQUIRED MIGRATION, run once against the live database before/after deploying this section's code** — this is a genuine schema migration, not just an edit to `d1/schema.sql`. `subscriptions` and `payments` both gained new columns below; `d1/schema.sql`'s `CREATE TABLE IF NOT EXISTS` is a no-op against the already-existing live tables, so those columns never appeared there automatically. This exact gap caused a real production 500 on `GET /api/client/me` (2026-09-07) — its new subscriptions query selects columns that didn't exist yet on the live DB, which D1 throws on and Cloudflare turns into an uncaught-exception 500. Run via the D1 Console (or `wrangler d1 execute <database-name> --remote --command "..."` per line):

```sql
ALTER TABLE payments ADD COLUMN source TEXT NOT NULL DEFAULT 'foryourbusiness_299' CHECK (source IN ('foryourbusiness_299', 'internal_upsell'));

ALTER TABLE subscriptions ADD COLUMN item_type TEXT NOT NULL DEFAULT 'plan' CHECK (item_type IN ('plan', 'addon'));
ALTER TABLE subscriptions ADD COLUMN item_id TEXT NOT NULL DEFAULT '';
ALTER TABLE subscriptions ADD COLUMN item_name TEXT NOT NULL DEFAULT '';
ALTER TABLE subscriptions ADD COLUMN billing_cycle TEXT NOT NULL DEFAULT 'one_time' CHECK (billing_cycle IN ('one_time', 'annual', 'monthly'));
ALTER TABLE subscriptions ADD COLUMN amount_php INTEGER NOT NULL DEFAULT 0;
ALTER TABLE subscriptions ADD COLUMN renewal_amount_php INTEGER;
ALTER TABLE subscriptions ADD COLUMN next_renewal_date TEXT;
ALTER TABLE subscriptions ADD COLUMN payment_id TEXT REFERENCES payments(id);

CREATE INDEX IF NOT EXISTS idx_subscriptions_client_id ON subscriptions(client_id);
```

Verified end to end against a local D1 instance seeded with the exact pre-migration schema (old `payments`/`subscriptions` shape, one pre-existing row each): all statements applied cleanly, the pre-existing rows backfilled with sane defaults (`item_type='plan'`, `amount_php=0`, `source='foryourbusiness_299'`, etc. — nothing broke on old rows), a fresh insert matching `functions/api/webhooks/internal-upsell.ts`'s exact column list succeeded, and the CHECK constraints correctly rejected an invalid `item_type` value. Only an *existing* database needs this manual step — a brand-new database created from the current `d1/schema.sql` picks up every column automatically via its `CREATE TABLE` statements, same as the `clients.welcome_dismissed_at` precedent in §9.

Post-presentation onward, ClientKeeper can now "reveal additional details" to the client in the form of a real Pricing page — transcribed from the operator's rate-card images (`ALTAVENTURES_RATES_2.zip`, 11 slides) into actual content, not raw images, per the operator's explicit instruction. Purchasing goes through a new, separate ganap.net project (`alta_internal_upsell`, credentials supplied in `Internal_Upsell.txt`) — not the `/foryourbusiness` project.

**Scope decisions, resolved via `AskUserQuestion` before any code was written** (three real forks, none obvious from the request alone):
1. **Purchasable scope:** the operator chose the largest option — every priced item on the rate card gets its own checkout, plans and add-ons alike (not just the 4 core plans). Items marked "Subject to quote" or "Client-paid + setup" (Custom Development, Additional Media Storage, High-Bandwidth, Additional Domain, Advanced SEO, Expedited Development, Complex/Scaled Systems) stay non-purchasable — there's no fixed number to charge for those without fabricating one, so they get a "Request a Quote" button that opens `ChatModal` instead, prefilled with the item name.
2. **Renewal model:** manual. ganap.net's documented API (`Internal_Upsell.txt`) is a one-time checkout session with no subscription/auto-charge endpoint — nothing bills a client automatically, ever. Instead, `subscriptions.next_renewal_date`/`renewal_amount_php` are computed at purchase time and surfaced on the Account page; the client clicks "Renew Now" when they're ready, which starts a fresh checkout for `renewal_amount_php` specifically (see the amount-bug note below).
3. **Offers vs. Pricing:** coexist. The existing staff-curated `offers` table/mechanism (a single targeted nudge in the dashboard's "What's Next" card) is untouched; Pricing is a separate, always-there nav page for self-serve browsing once unlocked. Guardrail #3 in §8 was amended accordingly — it used to forbid exposing "the full pricing table" to any client endpoint, written before this page existed.

**Master comparison grid (rate-card slide 1) is the transcription source of truth**, not the four individual per-plan slides (2-5) — those repeat most of the same content but occasionally disagree in small ways (e.g. the Starter slide's own "what's not included" list names "Support" as excluded, while the master grid's Starter row gives Support the value "Basic"); the master grid wins since it's the one head-to-head comparison meant to be read as a whole. The per-plan slides are used only for tagline copy.

**Data model:** `src/content/pricing.ts` (frontend display catalog: `PLANS`, `ADDON_CATEGORIES`, full included/excluded lists, taglines) and `functions/_lib/pricing.ts` (server catalog: id → `chargeNowPhp`/`renewalPhp`, the only source of truth for what a checkout actually charges) are deliberately two separate files with matching ids, not one shared import — this codebase's established convention keeps `functions/` and `src/` from cross-importing (see `src/lib/api.ts`'s own duplicated `Stage` type rather than importing `functions/_lib/stages.ts`'s). Add/rename an id in one file, update the other in the same change.

**`chargeNowPhp` vs `renewalPhp` — a real billing bug caught and fixed during this build:** for an annual plan (Essential/Business), `chargeNowPhp` bundles the upfront fee plus the first year combined (Essential: ₱1,500 + ₱4,200 = ₱5,700), matching the rate card's own "Upfront Payment" + "/year" split shown as two numbers on one card. The Account page's "Renew Now" button was first wired to call the same `/api/client/checkout-upsell` endpoint with just `{ itemId }` — which would have silently re-charged the full ₱5,700 (including the upfront fee a second time) on every renewal instead of just the ₱4,200 annual fee. Fixed by adding an `isRenewal` boolean to the request body: the endpoint charges `renewalPhp` instead of `chargeNowPhp` only when `isRenewal` is true *and* the item actually has a `renewalPhp` (a request for a non-renewing item's "renewal" just falls back to `chargeNowPhp`, which only matters for a direct API call bypassing the UI's own gated button). `AccountPage.tsx`'s `handleRenew` passes `isRenewal: true`; the Pricing page's "Get Started"/"Add" buttons don't. Basic's own renewal is its ₱750/year domain fee (its ₱1,500 build itself is genuinely one-time, per the rate card slide 3) — the same `isRenewal` flag handles this correctly too, since `findCatalogItem("basic").renewalPhp === 750`.

**One active plan at a time, add-ons stack:** `subscriptions.item_type` distinguishes `'plan'` from `'addon'`. The webhook (`functions/api/webhooks/internal-upsell.ts`) supersedes any existing active `'plan'` row (`status = 'cancelled'`, `ended_at = now`) before inserting the new one whenever a plan purchase lands — verified live: purchasing Business after Essential correctly left Essential `cancelled` and Business the sole `active` plan row. Add-ons never supersede each other; a client can hold any number of active add-on rows simultaneously.

**`functions/api/client/checkout-upsell.ts`** (`POST`, body `{ itemId, isRenewal? }`): looks up the amount from `functions/_lib/pricing.ts` server-side — never trusts a client-supplied amount, same posture as the marketing site's `/foryourbusiness` checkout. Re-checks the `post_presentation`-or-later stage gate independently of the UI (`FORWARD_SEQUENCE` from `functions/_lib/stages.ts`) — a client hitting this endpoint directly before that stage gets a `403`, not just a hidden button; verified live (`discovery` stage → `403`, `post_presentation` → passes through to the network call). Metadata sent to ganap.net includes `clientId` (how the webhook finds the right client — far more reliable than an email lookup for an already-authenticated existing client) and `itemId`.

**`functions/api/webhooks/internal-upsell.ts`**: same signature-verification/idempotency shape as the existing `functions/api/webhooks/ganap.ts`, confirmed against `Internal_Upsell.txt`'s documented payload. Writes a `payments` row (`source = 'internal_upsell'`, a new column distinguishing it from `'foryourbusiness_299'`), supersedes-or-inserts the `subscriptions` row as described above, logs a `client_activity` row, and sends a Resend confirmation email (`upsellPurchaseEmail()` in `functions/_lib/email.ts`) naming the renewal date/amount when the item has one. Idempotency verified live (replaying an identical signed payload left `subscriptions` at the same row count, no duplicate).

**IMPORTANT — operator action needed before this goes live:** the ganap.net dashboard for `alta_internal_upsell` is configured with webhook URL `https://altasme.com/internalupsell` (the *marketing site's* domain, per `Internal_Upsell.txt`). This handler instead lives in Client Hub, for the same reason the `/foryourbusiness` webhook was deliberately repointed from `altasme.com` to `account.altasme.com` once Client Hub existed (see the marketing site's own `CLAUDE.md` §19 correction) — a payment that updates a client's plan belongs with the client data it's updating, not duplicated into a database the marketing site has no reason to own. **Repoint this project's webhook URL in the ganap.net dashboard to `https://account.altasme.com/api/webhooks/internal-upsell`** before any real purchase can be processed; no deliveries reach this handler until that's done.

**`GET /api/client/me`** gained two fields, computed once and shared by both the Pricing page and the Account page's plan card: `pricingUnlocked` (mirrors the same stage gate the checkout endpoint enforces — this copy is display-only, a convenience, not the actual security boundary) and `subscriptions` (only ever `status = 'active'` rows). `Layout.tsx`'s nav only renders the "Pricing" link when `pricingUnlocked` is true.

**`src/pages/PricingPage.tsx`**: plan cards (with a "Your Current Plan" badge when an active subscription matches, per `activeByItemId` built from `me.subscriptions`) plus every add-on category from the rate card, each with its own Buy/Add/Request-Quote button depending on `purchasable`. A real bug was caught and fixed here during testing: an unconditional `useEffect(() => refresh(), [])` on mount caused an **infinite render loop** — `MeContext.tsx`'s `MeProvider` unmounts its children entirely while `status === 'loading'`, so calling `refresh()` on every fresh mount of `PricingPage` re-triggered the same mount effect forever once the fetch resolved and remounted the page. Fixed by removing the auto-refresh entirely (ganap.net's `successRedirectUrl` is a real browser navigation back to `/pricing`, not client-side routing, so `MeProvider` already fetches fresh `me` on that reload without any help) and replacing it with a manual "Refresh" link, since the webhook can still land a few seconds after the redirect (same lag already documented for the marketing site's `/foryourbusiness` thank-you page). Confirmed via Playwright: the request count to `/api/client/me` settled at exactly 1 after the fix (previously dozens within 3 seconds).

**`src/pages/AccountPage.tsx`** gained a "Your Plan" card (`YourPlanCard`), rendered only when `me.subscriptions.length > 0`: the active plan's name/price/billing cycle, a "Renews {date} — ₱{amount}" line with "Renew Now" when the plan has a `renewal_amount_php`, and a list of active add-ons with their own renewal lines/buttons where applicable.

**How this was tested:** live, end to end, against a local `wrangler pages dev` + local D1 instance (gitignored `wrangler.toml`/`.dev.vars`, deleted after the session, never committed) — `npx wrangler d1 execute --local --file=./d1/schema.sql` against a fresh local database, a hand-seeded test client/project at `post_presentation`, and a hand-constructed session cookie matching `functions/_lib/session.ts`'s exact HMAC algorithm (the same established pattern used throughout this project). Verified: `/api/client/me`'s `pricingUnlocked` flips correctly across stages; `/api/client/checkout-upsell` rejects an unknown item (`400`), rejects the pre-gate stage (`403`), and reaches the real ganap.net network call correctly for a valid item (`502` in this sandbox specifically, since `convex-top-api.ganap.net` is proxy-blocked here — the same limitation hit throughout this project's ganap.net work); the webhook rejects a forged signature (`401`) and accepts a correctly-signed `transaction.paid` payload (`200`), correctly writing `payments`/`subscriptions`/`client_activity` rows and correctly computing `next_renewal_date` one year out; a second, different plan purchase correctly cancelled the first plan's row while leaving it as the sole active one; a replayed identical webhook payload correctly no-op'd. Playwright screenshots confirmed the Pricing page renders all plans/add-ons with the correct "Your Current Plan" badge on the actually-active plan, and the Account page's "Your Plan" card shows the right name/price/renewal date/"Renew Now" button. **A note on this sandbox's browser testing specifically:** Chromium here was silently routing all `localhost`/`127.0.0.1` traffic through the sandbox's own agent proxy despite `NO_PROXY` listing loopback addresses, causing every `page.goto()` to hang until timeout — fixed by launching with `--no-proxy-server`; worth remembering for any future Playwright work in this environment.

**Known open items (not blocking, do not build speculatively):**
- **The real end-to-end ganap.net call was never verified live** (this sandbox cannot reach `convex-top-api.ganap.net`, same limitation as every other ganap.net integration in this ecosystem) — the operator should click through one real test-mode purchase once deployed, the same two-step verification pattern already used for the `/foryourbusiness` Payment Portal.
- **The ganap.net webhook URL still needs repointing** — see the "IMPORTANT" note above. Nothing purchased on this page reaches Client Hub's database until that's done.
- **Legal review**: the Digital Growth Plans pricing/terms shown here haven't been reviewed by a lawyer, same caveat already standing for the `/foryourbusiness` ₱299 offer's terms.
- **No proration or mid-cycle plan-change logic.** Switching from Essential to Business mid-year charges the full Business `chargeNowPhp` (upfront + first year) again rather than crediting the unused portion of Essential — acceptable for V1 given how rarely a mid-year switch is expected, but worth a deliberate decision if it comes up for real.
- **No Resend "renewal due soon" reminder email yet** — `next_renewal_date` is tracked and displayed, but nothing proactively emails the client as it approaches. Add one if the operator wants it; don't build it speculatively before they ask.

---

## 13. Dashboard/Pricing/Account refinements + Starter-plan auto-assignment [2026-09-07]

A second round of feedback on §11/§12, all in `DashboardPage.tsx`/`PricingPage.tsx`/`AccountPage.tsx` unless noted:

- **Presentation stage meeting link is no longer withheld until the day of presentation.** §11's `isPresentationToday()` gate is removed entirely — the meeting link now shows as soon as staff set it in ClientKeeper, regardless of the scheduled date. Verified live: a presentation dated over a year in the future with a meeting link set correctly showed "Join the call →" immediately (previously this stayed hidden until the calendar day matched).
- **The "What's Next" badge is now "We're Live!"**, and the post-presentation card (stages `post_presentation`/`offer_unlocked`/`conversion`/`essential_upsell`) was restructured to always show: the headline "Your website is now live!", the live `project.websiteUrl` as a real link (when set), a "✓ Online and up 24/7" line, and a "Got questions? → Chat with Your Developer" block — all unconditionally, not just in the no-offer fallback. A staff-curated `offers` row (when one exists) now renders as an additional section below the live-site info rather than replacing it, so the "your site is live" facts are never hidden behind a commercial offer. The old "We'll reach out with next steps shortly." line is gone.
- **Add-ons are chat-only, not self-checkout** — walked back from §12's "core plans + add-ons all purchasable" decision. Every add-on's price is still shown (transparency), but the CTA is now uniformly "Chat with Developer" (`openAddonChat()`, a price-aware WhatsApp/Messenger/Viber prefill via `ChatModal`) regardless of `item.purchasable`; the `handleBuy()`/checkout-upsell path is now only reachable from the 4 core plan cards. The backend (`functions/api/client/checkout-upsell.ts`, `functions/_lib/pricing.ts`) is untouched — add-on catalog entries still exist and are still technically chargeable by id, just not reachable from any UI button anymore.
- **"Payment is not configured yet" diagnosis**: this error (seen live) meant `GANAP_INTERNAL_UPSELL_SECRET`/`GANAP_INTERNAL_UPSELL_PROJECT_UUID`/`DB` weren't all set on the Cloudflare Pages project — see §7.1. `checkout-upsell.ts` now logs exactly which of the three is missing (`console.error`) instead of a generic message, so this is diagnosable from Cloudflare's Functions logs next time without guessing.
- **Account page "Your Plan" card now shows what the plan actually includes** — the full `included` feature checklist from `content/pricing.ts`'s matching `PLANS` entry (looked up by `subscriptions.item_id` via the exported `findPlan()`), not just the name/price/renewal info §12 originally shipped. Renders nothing extra if the plan id isn't found in the catalog (defensive — a staff-overridden or legacy plan id should never crash the page).
- **Every new client now starts on the Starter Plan automatically.** `functions/api/webhooks/ganap.ts` (the `/foryourbusiness` ₱299 webhook) now writes an active `subscriptions` row (`item_id: "starter"`, looked up from `functions/_lib/pricing.ts` rather than hardcoded) alongside the existing client/project/payment rows it already created — but only for a genuinely new client (`!existingClient`); a returning client paying for a second project keeps whatever plan history they already have. Verified live: a fresh webhook payload for a brand-new email correctly produced an active Starter Plan subscription row.
- **Staff can override a client's plan** — see ClientKeeper's CLAUDE.md §7 for the `set-plan` endpoint this pairs with. `me.subscriptions` and the Account/Pricing pages read the resulting row exactly the same way whether it came from a real ganap.net purchase or a staff override (the only difference is `payment_id` is `NULL` on an override row).

**How this was tested:** live, end to end, against a local `wrangler pages dev` + local D1 instance (gitignored `wrangler.toml`/`.dev.vars`, deleted after the session, never committed). Simulated a brand-new client's `/foryourbusiness` webhook and confirmed the Starter Plan subscription row; simulated an Essential Plan purchase via the internal-upsell webhook and confirmed the Account page renders its full feature list + renewal date; Playwright screenshots confirmed the presentation stage's meeting link shows regardless of date, the post-presentation card matches the new copy exactly (badge, live link, checkmark line, "Got questions?", no leftover "We'll reach out" text), and every Pricing page add-on shows a price + "Chat with Developer" button with no "Add" buttons remaining.

---

## 14. URL scheme bug, downgrade rule, and Basic plan pricing correction [2026-09-07]

Three fixes/changes on top of §13, all found from real usage:

**Website URL relative-link bug.** A URL entered without a scheme (e.g. `imago.altasme.com`, typed into ClientKeeper's website field) rendered as `<a href="imago.altasme.com">`, which browsers resolve as a *relative* URL against the current page — it opened `https://account.altasme.com/imago.altasme.com` instead of `https://imago.altasme.com`. Fixed in two places: canonically, ClientKeeper's `functions/api/app/projects/[id]/website.ts` now normalizes on save (prepends `https://` if the value doesn't already start with `http(s)://`); defensively, `src/lib/url.ts`'s `ensureAbsoluteUrl()` is applied at every render site in both apps (clienthub's `DashboardPage.tsx`/`WebsitePage.tsx`, ClientKeeper's `ClientDetailPage.tsx`) so a URL already stored without a scheme before this fix self-heals without needing a re-save. Verified live: a project seeded with `website_url = 'imago.altasme.com'` (the un-normalized legacy shape) rendered `href="https://imago.altasme.com"` correctly on the client Dashboard.

**Clients cannot downgrade themselves, and the UI never tells them the concept exists.** Every client already has an active plan (Starter, at minimum, per §13's auto-assignment) — a move to a lower tier is always a downgrade, never a first purchase, and only staff can do that (ClientKeeper's `set-plan` override, §7 there — unrestricted by design, staff are fully trusted). Two layers, per this project's "hiding in the UI is a convenience, the real boundary is server-side" convention:
- **Real boundary:** `functions/api/client/checkout-upsell.ts` looks up the client's current active plan's tier (`PLAN_TIERS` — a new export in `functions/_lib/pricing.ts`: `starter: 0, basic: 1, essential: 2, business: 3`) and rejects (`403`) any plan purchase whose tier is lower, whether or not the UI would ever have offered it. A renewal of the client's own current plan is never affected (same tier, never less than). The rejection message is deliberately generic ("This plan isn't available for your account right now") — it doesn't name "downgrade" as a concept, since a client hitting this at all only got there via a direct API call the UI never exposes.
- **Display:** `PricingPage.tsx` doesn't show a disabled button or a "contact us to downgrade" message for a lower-tier plan (an earlier pass tried that and the operator explicitly rejected it — **"Dont put a contact us to downgrade. We dont let the customers know that"**) — it simply doesn't render the plan card at all for a client whose current tier is higher. `visiblePlans` filters `PLANS` down to tiers `>= currentTier` before mapping (Custom Development, which has no tier, always shows). Verified live: an Essential-plan client's Pricing page rendered only Essential/Business/Custom — Starter and Basic cards were completely absent, not disabled.

**Basic plan pricing corrected** (the operator's own words: "Basic plan is not 1500 one time. It's now 1500 yearly (Basic domains only — premium domains subject to quote)"): Basic changed from a ₱1,500 one-time build + a separate ₱750/year domain-renewal line to a single **₱1,500/year** plan, `billing: "annual"`, `chargeNowPhp: 1500`, `renewalPhp: 1500` — no upfront component, unlike Essential/Business. Updated in both `content/pricing.ts` and `functions/_lib/pricing.ts` here, and ClientKeeper's `functions/_lib/pricing.ts` + `PLAN_OPTIONS` display label. **Domain renewal is now described as bundled into every plan's own annual fee** (all three paid plans' `domainRenewalNote` reads "Includes domain renewal (basic domains). Premium domains subject to quote.") rather than tracked as a separate figure — resolving the ambiguity §12 had flagged as a deliberate simplification; the operator's correction confirms renewal was never meant to be a second line item. A premium (non-basic) domain costs more than this, but that's a quote, not a fixed catalog price, so it isn't modeled as its own SKU.

**Known gap, not yet fixed at the time of this note:** clients who existed *before* §13's auto-Starter-assignment shipped have no `subscriptions` row at all, so their Account page shows nothing under "Your Plan" — this looked like the feature was never built, but it's a data gap, not a code bug (confirmed live: `me.subscriptions` returns `[]` for such a client even though `pricingUnlocked` is correctly `true`). The fix is a one-time backfill, given to the operator to run against the live database (not run automatically by this codebase, same as every other D1 migration in this project):

```sql
INSERT INTO subscriptions (id, client_id, plan, status, started_at, item_type, item_id, item_name, billing_cycle, amount_php, payment_id)
SELECT lower(hex(randomblob(16))), c.id, 'Starter Plan', 'active', c.created_at, 'plan', 'starter', 'Starter Plan', 'one_time', 299, NULL
FROM clients c
WHERE NOT EXISTS (
  SELECT 1 FROM subscriptions s WHERE s.client_id = c.id AND s.item_type = 'plan' AND s.status = 'active'
);
```

Verified idempotent (safe to re-run — the `NOT EXISTS` guard skips clients who already have an active plan, whether from a real purchase or this same backfill) and correct (a client with no plan row gets exactly one Starter Plan row; the Account page then renders it immediately).

**Payment configuration checklist [operator action, 2026-09-07]:** the "Payment is not configured yet" error reported live traces to missing Cloudflare Pages environment variables — `GANAP_INTERNAL_UPSELL_SECRET` and `GANAP_INTERNAL_UPSELL_PROJECT_UUID` (credentials from `Internal_Upsell.txt`: the project `alta_internal_upsell`, secret `whsec_...`, project UUID `78314cd0-3b22-4f1b-bf91-44faf1355a3a`) were never set on the `clienthub` Cloudflare Pages project. Two separate things both need to be done before a real purchase can complete, and neither is something this codebase can do for itself:
1. Set `GANAP_INTERNAL_UPSELL_SECRET` and `GANAP_INTERNAL_UPSELL_PROJECT_UUID` in the Cloudflare Pages dashboard (clienthub project → Settings → Environment variables → Production, and Preview if used), then trigger a new deployment (env var changes need a fresh deployment to take effect for Pages Functions).
2. Repoint the `alta_internal_upsell` ganap.net project's webhook URL from `https://altasme.com/internalupsell` (its current dashboard setting, the marketing site's domain) to `https://account.altasme.com/api/webhooks/internal-upsell` — see §12's "IMPORTANT" note; this was already flagged there and doesn't yet appear to be done.

**How this was tested:** live, end to end, against a local `wrangler pages dev` + local D1 instance (gitignored `wrangler.toml`/`.dev.vars`, deleted after the session, never committed). Confirmed the URL fix, the downgrade rejection (`403` for Basic/Starter from a Business-plan client, passthrough-to-network for a same-plan renewal), the visible-plans filtering (Essential-plan client sees only Essential/Business/Custom), and the backfill SQL (idempotent, correctly assigns Starter to a client with no plan row, Account page picks it up immediately).
